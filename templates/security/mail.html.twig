<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Envoi de mail</title>
    <style>

        body {
            margin: 6rem;
            background-color: #e7e7e7;
        }

        .back-button {
            font-family: sans-serif;
            font-weight: bold;
            color: #000;
            text-decoration: none;
            border: 1px solid white;
            outline: none;
            background-color: #e7e7e7;
            padding: 1rem 2rem;
            border-radius: .5rem;
            cursor: pointer;
            transition: .3s ease-in-out;
        }

        .back-button:hover {
            background-color: #FFF;
        }

        h2 {
            font-family: sans-serif;
            font-weight: bold;
            margin-top: 3rem;
        }

        form {
            display: flex;
            flex-direction: column;
            background-color: #FFF;
            box-shadow: 0 0 .5rem rgba(0, 0, 0, .3);
            border-radius: .5rem;
            width: 100%;
            max-width: 24rem;
            padding: 2rem;
        }

        form p {
            font-family: sans-serif;
            font-size: 1rem;
            margin: 1.5rem 0 1rem .5rem;
        }

        form input, form select {
            font-family: sans-serif;
            background-color: white;
            border: 1px solid #e0e0e0;
            padding: .5rem;
            border-radius: .5rem;
            outline: none;
        }

        .form__button {
            font-family: sans-serif;
            font-weight: bold;
            border: none;
            outline: none;
            background-color: royalblue;
            color: white;
            padding: 1rem 2rem;
            border-radius: .5rem;
            cursor: pointer;
            margin: 2rem auto 0 auto;
            width: fit-content;
            transition: .3s ease-in-out;
        }

        .form__button:hover {
            background-color: #2745a0;
        }

    </style>
</head>
<body>
    <a href="/admin" class="back-button">Retour</a>
    <h2>Envoyer un mail d'inscription</h2>
    {{ form_start(form) }}
        <p>Adresse mail du destinataire</p>
        {{ form_widget(form.email) }}
        <p>Membre de l'équipe pédagogique ?</p>
        {{ form_widget(form.isEduc) }}
        {{ form_widget(form.save) }}
    {{ form_end(form) }}
</body>
</html>